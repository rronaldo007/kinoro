"""graph — typed-port node graph evaluator. Post-MVP.

Design modeled on Olive's ``app/node/`` typed-port pull-based eval with per-time
cache (research/olive/app/node/). React Flow renders the graph in the UI.
"""
